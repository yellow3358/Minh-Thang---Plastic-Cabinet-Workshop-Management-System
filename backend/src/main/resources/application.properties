spring.application.name=backend
# ==============================================================
# 1. DATASOURCE CONFIG (SUPABASE)
# ==============================================================
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.bicupmuqmzeexpelknjq}
spring.datasource.password=${DB_PASSWORD:DoAnTotNghiep}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# 2. JPA / HIBERNATE CONFIG
# ==============================================================
# 'update': T? ??ng s?a b?ng n?u code thay ??i (Ch? d?ng l?c Dev)
# 'validate': Ch? ki?m tra xem code c? kh?p DB kh?ng (Khuy?n d?ng v? b?n ?? t?o DB r?i)
spring.jpa.hibernate.ddl-auto=update

# Hi?n th? c?u l?nh SQL trong Console ?? d? debug
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialect chu?n cho Postgres
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Gi?i h?n s? l??ng k?t n?i t?i ?a xu?ng 5 (M?c ??nh l� 10, r?t d? g�y ??y DB Cloud)

# ==============================================================
# 3. SERVER CONFIG
# ==============================================================
server.port=8080
# Cho ph?p hi?n th? message l?i chi ti?t (d? debug)
spring.web.error.include-message=always
# ==============================================================
# 4. JWT CONFIGURATION
# ==============================================================
# ??y l? chu?i b? m?t ?? k? Token (Ph?i ?? d?i v? ng?u nhi?n)
app.jwtSecret=${JWT_SECRET:SuperSecretKeyForThesisProject2026MustBeVeryLongAndSecureToAvoidErrors}
# Th?i gian h?t h?n Token (ms) - V? d?: 86400000 = 1 ng?y
app.jwtExpirationMs=36000000
# ===============================
# C?U H?NH G?I EMAIL (GMAIL SMTP)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:hanzoalizer@gmail.com}
spring.mail.password=${MAIL_PASSWORD:aqjd iqnl tzzl rktv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Cho ph�p upload ?nh t?i ?a 5MB
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

#payos
payos.client-id=${PAYOS_CLIENT_ID:91bcc904-65ae-4f04-8f14-3a8ff4fa4edc}
payos.api-key=${PAYOS_API_KEY:00a2efb7-322d-428e-a301-df5732f24e5b}
payos.checksum-key=${PAYOS_CHECKSUM_KEY:42ffb9d8263ca2bbb3fc007df417ecd0a423af4b4568df9ee27fa889b3b869bb}

# URL redirect sau khi thanh to�n xong (trang k?t qu? tr�n FE)
payos.return-url=${PAYOS_RETURN_URL:http://localhost:5173/payment/result}
payos.cancel-url=${PAYOS_CANCEL_URL:http://localhost:5173/payment/cancel}

spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.threshold-for-idle-connection-validation=3000